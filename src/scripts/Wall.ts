class Wall {
    public x: number;
    public y: number;
    public erasable: boolean;

    constructor(x: number, y: number, erasable: boolean) {
        this.x = x;
        this.y = y;
        this.erasable = erasable;
    }
}